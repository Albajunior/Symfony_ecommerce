{% extends 'base.html.twig' %}

{% block title %}Liste Category{% endblock %}

{% block body %}

    <main class="container">
        <section class="row"> 
            <div class="col-12">
                 <h1> Liste des produits {{ category.name }} </h1>
           
            {# la on va recuperer juste les produitsdu category concerner #}
            {% for product in products %}
           
                    <div class="col-4"> 
                        <div class="card h-100">
                            <div class="card-body">
                                <h2 class="card-title">{{ product.name }}</h2>
                                <p class="card-title">{{ product.description }}</p>
                                <a hrref="#" class="btn btn-primary"> {{ product.price }} $</a>
                            </div>
                        </div>
                    </div>

                    {# <div class="col-4"> 
                        <div class="card h-100">
                            <div class="card-body">
                                <h3 class="card-title">{{ category.name }}</h3>
                                <a href="{{ path ('category_liste', {slug: category.slug}) }}" class="btn btn-primary"> Voir plus</a>
                            </div>
                        </div>
                    </div> #}
                 
            {% endfor %} 

            {# <div class="col-12">
                {{ dump() }}
            </div> #}

            </div>
        </section>    
    </main>

{% endblock %}
